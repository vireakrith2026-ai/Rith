<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>VIREAK RITH — Official</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;700;900&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b0b0b;
      --muted:#9a9a9a;
      --accent:#ffffff;
      --glass: rgba(255,255,255,0.06);
      --card: rgba(255,255,255,0.03);
      --radius:18px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: "Montserrat", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:linear-gradient(180deg,#050505 0%, #0f0f0f 100%);
      color:var(--accent);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.3;
    }

    /* NAV */
    .nav{
      position:fixed;left:0;right:0;top:0;z-index:40;
      display:flex;align-items:center;justify-content:space-between;
      padding:18px 28px;
      background: linear-gradient(180deg, rgba(11,11,11,0.65) 0%, rgba(11,11,11,0.15) 100%);
      backdrop-filter: blur(6px);
      border-bottom:1px solid rgba(255,255,255,0.03);
    }
    .brand{display:flex;gap:12px;align-items:center}
    .logo{
      width:44px;height:44px;border-radius:8px;background:var(--card);
      display:flex;align-items:center;justify-content:center;font-weight:900;font-size:18px;
      color:var(--accent);
    }
    .brand h1{margin:0;font-size:14px;font-weight:700;letter-spacing:1px}
    .nav a{color:var(--muted);text-decoration:none;margin-left:18px;font-weight:500}
    .nav a.cta{background:var(--accent);color:#000;padding:8px 12px;border-radius:10px;font-weight:700;margin-left:22px}

    /* HERO */
    .hero{
      min-height:84vh;
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:48px;
      align-items:center;
      padding:120px 6vw 80px;
    }
    .hero-left{
      max-width:880px;
    }
    .eyebrow{color:var(--muted);font-weight:600;letter-spacing:2px;margin-bottom:12px}
    .headline{
      font-size:clamp(36px,6vw,64px);
      margin:0 0 18px 0;
      line-height:0.96;
      font-weight:900;
      letter-spacing:-1px;
    }
    .sub{color:var(--muted);font-size:16px;max-width:56ch;margin-bottom:22px}
    .buttons{display:flex;gap:12px}
    .btn{padding:12px 18px;border-radius:12px;background:transparent;border:1px solid rgba(255,255,255,0.08);color:var(--accent);font-weight:700}
    .btn.primary{background:var(--accent);color:#000;border:none}

    .hero-right{
      justify-self:end;
      align-self:center;
      width:100%;
      display:flex;
      justify-content:center;
    }
    .portrait{
      width:320px;height:420px;border-radius:22px;background-size:cover;background-position:center;
      box-shadow: 0 30px 60px rgba(0,0,0,0.7), 0 6px 18px rgba(255,255,255,0.02) inset;
      border:1px solid rgba(255,255,255,0.04);
    }

    /* SECTIONS */
    section{padding:72px 6vw; border-top:1px solid rgba(255,255,255,0.02)}
    .container{max-width:1200px;margin:0 auto}

    /* STATS */
    .stats{display:flex;gap:18px;flex-wrap:wrap}
    .stat{flex:1 1 180px;background:var(--card);padding:20px;border-radius:14px;text-align:center}
    .stat .num{font-size:28px;font-weight:900}
    .stat .label{color:var(--muted);font-weight:700;margin-top:6px}

    /* GALLERY */
    .gallery{
      display:grid;grid-template-columns:repeat(3,1fr);gap:12px;
    }
    .gallery img{width:100%;height:220px;object-fit:cover;border-radius:12px}

    /* NEWS */
    .news-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
    .card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));padding:18px;border-radius:14px}
    .card h3{margin:0 0 10px 0}
    .card p{color:var(--muted);font-size:14px}

    /* CONTACT */
    form{display:grid;grid-template-columns:1fr 1fr;gap:12px;max-width:860px}
    input,textarea{background:transparent;border:1px solid rgba(255,255,255,0.06);padding:12px;border-radius:10px;color:var(--accent)}
    textarea{grid-column:1/-1;min-height:140px;resize:vertical}
    button[type="submit"]{grid-column:1/-1;padding:12px;border-radius:12px;border:none;background:var(--accent);color:#000;font-weight:800}

    /* FOOTER */
    footer{padding:36px 6vw;color:var(--muted);border-top:1px solid rgba(255,255,255,0.02);text-align:center}

    /* Responsive */
    @media (max-width:960px){
      .hero{grid-template-columns:1fr; padding:100px 6vw 40px}
      .hero-right{order:-1;margin-bottom:20px}
      .portrait{width:100%;height:420px}
      .news-grid{grid-template-columns:1fr}
      .gallery{grid-template-columns:repeat(2,1fr)}
      form{grid-template-columns:1fr}
    }
    @media (max-width:520px){
      .nav{padding:12px 16px}
      .nav a{display:none}
      .headline{font-size:30px}
    }
  </style>
</head>
<body>

  <header class="nav">
    <div class="brand">
      <div class="logo">VR</div>
      <div>
        <h1>VIREAK RITH</h1>
        <div style="font-size:12px;color:var(--muted)">Official Website</div>
      </div>
    </div>

    <nav style="display:flex;align-items:center">
      <a href="#about">About</a>
      <a href="#media">Media</a>
      <a href="#stats">Stats</a>
      <a href="#news">News</a>
      <a href="#contact" class="cta">Contact</a>
    </nav>
  </header>

  <main>
    <section class="hero" aria-label="Hero">
      <div class="hero-left">
        <div class="eyebrow">OFFICIAL</div>
        <h2 class="headline">VIREAK RITH — Creating impact through design & performance</h2>
        <p class="sub">I’m Vireak Rith — [short description: e.g., athlete, creator, entrepreneur]. This is my official site where I share projects, media highlights and ways to connect. Replace this with your short personal intro.</p>
        <div class="buttons">
          <a href="#about" class="btn">About</a>
          <a href="#contact" class="btn primary">Get in touch</a>
        </div>
      </div>

      <div class="hero-right">
        <!-- Replace the background-image URL with your portrait/photo -->
        <div class="portrait" style="background-image:url('https://images.unsplash.com/photo-1524504388940-b1c1722653e1?q=80&w=1400&auto=format&fit=crop&s=placeholder');"></div>
      </div>
    </section>

    <section id="about">
      <div class="container">
        <h2>About</h2>
        <p style="color:var(--muted);max-width:70ch">Write a compelling biography: highlights, notable projects/awards, and a brief timeline. Keep the tone confident and concise — think of lines a fan or client would want to read first.</p>
      </div>
    </section>

    <section id="media">
      <div class="container">
        <h2>Media / Gallery</h2>
        <div class="gallery" aria-hidden="false">
          <!-- Replace these src with your images -->
          <img src="https://images.unsplash.com/photo-1515879218367-8466d910aaa4?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 1">
          <img src="https://images.unsplash.com/photo-1504198453319-5ce911bafcde?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 2">
          <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 3">
          <img src="https://images.unsplash.com/photo-1506956191973-9edc7f30c1a1?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 4">
          <img src="https://images.unsplash.com/photo-1489987707025-afc232f7ea0f?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 5">
          <img src="https://images.unsplash.com/photo-1508921912186-1d1a45ebb3c1?q=80&w=1200&auto=format&fit=crop&s=placeholder" alt="media 6">
        </div>
      </div>
    </section>

    <section id="stats">
      <div class="container">
        <h2>Achievements / Stats</h2>
        <div class="stats">
          <div class="stat"><div class="num">120+</div><div class="label">Projects</div></div>
          <div class="stat"><div class="num">15</div><div class="label">Years Experience</div></div>
          <div class="stat"><div class="num">50k</div><div class="label">Followers</div></div>
          <div class="stat"><div class="num">12</div><div class="label">Awards</div></div>
        </div>
      </div>
    </section>

    <section id="news">
      <div class="container">
        <h2>News</h2>
        <div class="news-grid">
          <article class="card">
            <h3>Latest release</h3>
            <p class="muted">Short summary about a new project, article or announcement. Date • tag</p>
          </article>
          <article class="card">
            <h3>Another highlight</h3>
            <p class="muted">Short summary about events, partnerships, or media appearances.</p>
          </article>
          <article class="card">
            <h3>Interview</h3>
            <p class="muted">Short summary with link to the full interview or post.</p>
          </article>
        </div>
      </div>
    </section>

    <section id="contact">
      <div class="container">
        <h2>Contact</h2>
        <p style="color:var(--muted);max-width:70ch">Booking, collaborations, or press — send a quick message.</p>
        <form onsubmit="event.preventDefault(); alert('Thanks! Message sent (demo). Replace with real backend).');">
          <input type="text" placeholder="Your name" required />
          <input type="email" placeholder="Email" required />
          <input type="text" placeholder="Subject" />
          <textarea placeholder="Message" required></textarea>
          <button type="submit">Send message</button>
        </form>
      </div>
    </section>

  </main>

  <footer>
    <div style="max-width:1000px;margin:0 auto">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap">
        <div>© <strong>VIREAK RITH</strong> — Official</div>
        <div style="color:var(--muted)">Follow: [Instagram] [Twitter] [YouTube]</div>
      </div>
    </div>
  </footer>

  <script>
    // small enhancement: smooth scroll for nav links
    document.querySelectorAll('a[href^="#"]').forEach(a=>{
      a.addEventListener('click', (e)=>{
        const id = a.getAttribute('href');
        if(id.length>1){
          e.preventDefault();
          document.querySelector(id).scrollIntoView({behavior:'smooth', block:'start'});
        }
      });
    });
  </script>
</body>
</html>
